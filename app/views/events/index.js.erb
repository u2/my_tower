$('.events').append('<%= j render :partial => "events", :locals => { :events => @events } %>');
$('a.load-more-events').attr('href', '#{team_events_path(team_id: @team.id, till_id: @events.last.id}');

<% unless @events.next_page %>
  $('a.load-more-events').remove();
<% end %>